<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace必须与接口的全类名一致 -->
<mapper namespace="com.yy.mapper.UserMapper">
	<!-- id必须和mapper接口对应的功能名称一致 ，此处只写save功能 -->
	<insert id="save" parameterType="User">
		insert into user(nickname,
		username, password) values(#{nickname},#{username},#{password})
	</insert>
	<select id="findById" parameterType="String" resultType="User">
		select *
		from user where id = #{id}
	</select>
	<select id="findByUserName" parameterType="String" resultType="User">
		select * from user where username = #{username}
	</select>
	<delete id="delete" parameterType="User">
		delete from user where id =
		#{id}
	</delete>
	<update id="update" parameterType="User">
		update user set nickname=#{nickname},username= #{username},password= #{password} where id =
		#{id}
	</update>

</mapper>  